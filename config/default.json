{
	// All of the "config" entry will be sent to the client. No secrets allowed here!
	"config": {		
		"port": 8000,
		
		// User Credentials
		"login_name_min_length": 3,
		"login_name_max_length": 32,
		"login_name_allowed_regex": ".*",

		"login_pass_min_length": 8,
		
		// Room configuration
		"room_create_requires_admin": false,
		"room_max_rooms_per_user": 1,
		"room_max_lifespan_days": 30,
		"room_max_users_per_room": 30,
		
		// Encryption config
		"scrypt_N": 4096,
		"scrypt_r": 16,
		"scrypt_p": 2,
		"scrypt_dkLen": 32,
	},
	"server": {
		// SSL
		"key": null,
		"cert": null,
		"CA": null,
	}
}
